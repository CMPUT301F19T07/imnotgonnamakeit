package com.example.feelslikemonday.DAO;

/**
 * This interface contains a callback method. This callback passes a Boolean for other objects to work with
 */
public interface BooleanCallback {
    void onCallback(Boolean bool);
}
